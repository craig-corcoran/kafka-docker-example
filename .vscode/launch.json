{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Docker: Attach to Node",
            "type": "node",
            "request": "attach",
            "port": 5858,
            "address": "localhost",
            "localRoot": "${workspaceRoot}",
            "remoteRoot": "/app/example_service",
            "protocol": "inspector",
            "restart": true
        }
    ]
}